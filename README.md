# PortableRaylibTemplate
This is a template with a Makefile that compiles a game using raylib-6.0_win32_mingw-w64 and w64devkit contained on an external D: drive. The idea is to maximize portability so any device with VSCode or VSCodium can compile the game when the external drive is plugged in.
